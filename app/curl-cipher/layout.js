// app/curl-cipher/layout.js
import { metadata as curlCipherMetadata } from "./metadata";

export const metadata = curlCipherMetadata;

export default function CurlCipherLayout({ children }) {
  return <>{children}</>;
}
